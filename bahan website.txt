import re
from playwright.sync_api import Playwright, sync_playwright, expect


def run(playwright: Playwright) -> None:
    browser = playwright.chromium.launch(headless=False)
    context = browser.new_context()
    page = context.new_page()
    page.goto("https://antrean.logammulia.com/login")
    page.locator("iframe[src=\"https://challenges.cloudflare.com/cdn-cgi/challenge-platform/h/g/turnstile/f/ov2/av0/rch/kngla/0x4AAAAAAAAjq6WYeRDKmebM/light/fbE/new/normal?lang=auto\"]").content_frame.locator("body").dblclick()
    page.locator("iframe").content_frame.get_by_role("textbox", name="Username").click()
    page.locator("iframe").content_frame.get_by_role("textbox", name="Username").fill("pangkassobo@gmail.com")
    page.locator("iframe").content_frame.get_by_role("textbox", name="Password (klik lihat)").fill("198456z")
    page.locator("iframe").content_frame.get_by_role("spinbutton", name="Berapa hasil perhitungan dari").fill("-7")
    page.locator("iframe").content_frame.get_by_role("spinbutton", name="Berapa hasil perhitungan dari").press("Enter")
    page.locator("iframe").content_frame.get_by_role("button", name="Log in").click()


    # ---------------------
    context.close()
    browser.close()


with sync_playwright() as playwright:
    run(playwright)
